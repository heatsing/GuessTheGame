# Project Status — Guess the Game

## Current Phase

**Phase 1–3: Product & Design Definition** — COMPLETE
**Phase 4d: Game UI Wiring** — COMPLETE (slices 4d-1 through 4d-8 delivered; awaiting owner acceptance)

## Phase Tracker

| Phase | Role | Deliverable | Status | Commit |
|-------|------|-------------|--------|--------|
| 1 | PM | PRD + game rules + acceptance criteria | done | docs: define product requirements and game rules |
| 2 | Architect | Static architecture + DECISIONS | done | docs: define static application architecture |
| 3 | UX Designer | Sitemap + user flows + component states | done | docs: define user experience and interface system |
| 4a | Engineer | Application shell | done | chore: initialize static application shell |
| 4b | Content Architect | Puzzle content system | done | feat: add validated static puzzle content system |
| 4c | Engine Engineer | Game engine | removed | reverted — engine deferred |
| 4d | Engineer | UI implementation | done | feat: wire daily mixed challenge dashboard (4d-8) |
| 4e | Storage Engineer | Client data persistence | done | feat: add versioned local progress and statistics |
| 4f | SEO Engineer | Technical SEO & discovery | done | feat: add technical seo and static discovery pages |
| 4g | Perf & A11y Engineer | Loading & accessibility | done | perf: improve loading and accessibility |
| 4h | QA Engineer | Gameplay regression coverage | done | test: add comprehensive gameplay regression coverage |
| 4i | Security Reviewer | Security, privacy & copyright review | done | docs: add security privacy and copyright review |
| 4j | DevOps Engineer | Release pipeline | done | ci: add validated static deployment pipeline |
| 4k | Chief Reviewer | Final code review (8 subagents) | done | docs: add final code review |
| 4l | Acceptance Lead | Phase wrap-up + in-scope fixes | done | chore: phase 4e-4j acceptance and guardrails |
| 4m | Engineer | Comprehensive review fixes (P0/P1/P2) | done | fix: resolve all P0/P1 and actionable P2 review findings |
| 4n | Engineer | P2 polish batch (deferred cleanup) | done | refactor: centralize tokens, remove dead code, extract test helpers |
| 4d-1 | Engineer | Scoring engine (pure functions, PRD §7.1) | done | feat: add per-mode scoring engine + tests |
| 4d-2 | Engineer | Puzzle selection (seed + daily hash) | done | feat: add deterministic puzzle selection + tests |
| 4d-3 | Engineer | KeywordsGame component + /play/keywords wiring | done | feat: wire Keywords game board with scoring + storage |
| 4d-4 | Engineer | EmojiGame component + /play/emoji wiring | done | feat: wire Emoji game board with hints + scoring |
| 4d-5 | Engineer | ScreenshotGame component + /play/screenshot wiring (P1-10 blur tiers) | done | feat: wire Screenshot game board with blur-tier sharpening |
| 4d-6 | Engineer | TimelineGame component + /play/timeline wiring | done | feat: wire Timeline game board with reorder + position-error scoring |
| 4d-7 | Engineer | P1-10 next-clue preload + image compression (blurSrc LQIP + size caps) | done | perf: add image preload hook, LQIP blurSrc, and size-cap enforcement |
| 4d-8 | Engineer | /daily Daily Mixed Challenge page (dashboard + share) | done | feat: wire daily mixed challenge dashboard |

## Key Decisions Log

- 2026-07-09: Product pivoted from old "GuessVerse" (Semantic Guess + geography games) to "Guess the Game" (5-mode guessing platform). Local codebase deleted; GitHub repo reset to empty.
- 2026-07-09: Confirmed static-only architecture — no database, no login, no runtime backend.
- 2026-07-09: PRD complete — 5 game modes (Daily Mixed Challenge, Keywords, Emoji, Screenshot, Timeline), 45 acceptance criteria, 22 user stories.
- 2026-07-09: Architecture complete — Next.js App Router static export, 8 ADRs, engine/UI separation, localStorage data layer with schema migration.
- 2026-07-09: UX design complete — 12-page sitemap, full user flows, design tokens, 30+ component inventory, accessibility spec.
- 2026-07-09: Infrastructure shell complete — Next.js 15.5.20, `output: 'export'` enabled, build wrapper handles Windows Defender phantom-file bug, dark theme via `color-scheme: dark`.
- 2026-07-09: Game engine (Phase 4c) reverted/removed at owner request. Content system simplified — removed `PuzzleSchema` discriminated union in favor of per-mode `schemaForMode`/`parsePuzzle` dispatch. Engine will be re-introduced when needed for UI implementation.
- 2026-07-09: Client data persistence (Phase 4e) complete — `src/storage/` module with unified Storage Adapter (localStorage + memory fallback), schema versioning (`CURRENT_SCHEMA_VERSION = 2`), V1→V2 chained migration, JSON corruption recovery (corrupt data parked to `:corrupted` key, never overwritten), SSR-safe reads/writes, capacity pruning (daily 60d / last30 30d / recent 20), idempotent domain actions (`recordModeResult` no-op on duplicates, no score downgrade), export/reset, 84 unit + integration tests.
- 2026-07-09: Technical SEO (Phase 4f) complete — per-page unique title/description/canonical/OG via Next.js Metadata API, `app/sitemap.ts` + `app/robots.ts`, WebSite/WebApplication/FAQPage JSON-LD (no fake reviews), static H1 + how-to content on every play page, `/categories` discovery page, related-mode internal links, noindex on thin pages (stats, share, archive/[date], 404), canonical domain centralized in `src/lib/site-config.ts` via `NEXT_PUBLIC_SITE_URL`. 144 tests pass, static build verified.
- 2026-07-09: Performance & accessibility (Phase 4g) complete — `prefers-reduced-motion` honored (animations/smooth-scroll disabled), `touch-action: manipulation` + `-webkit-tap-highlight-color` on interactive elements, `overscroll-behavior: contain` on modals, `scroll-margin-top` on heading anchors, Modal now marks background `inert` + supports `aria-describedby`, new accessible game primitives (`GameImage` fixed-dim + lazy/priority + error fallback, `ResultAnnouncer` aria-live non-color result, `TimelineControls` non-drag keyboard reorder, `LazyPlayMode` code-splitting, `ShareButton` Web Share→clipboard fallback). 203 unit tests + 3 e2e pass.
- 2026-07-09: QA regression coverage (Phase 4h) complete — pure `src/lib/game/match.ts` (answer normalization / correct guess / duplicate detection / wrong-guess dedup) with 21 tests; component tests for GameImage/ResultAnnouncer/TimelineControls/ShareButton/Modal a11y/not-found; storage edge cases (daily date boundary, missing puzzle reference, empty bank, score floor, refresh recovery, reset); Playwright config + 3 e2e (homepage H1, keyboard nav to play page, 404 quick links). 203 unit + 3 e2e green; content:check + build verified.
- 2026-07-09: Security/privacy/copyright review (Phase 4i) complete — read-only audit in `docs/SECURITY-REVIEW.md`. 1 High (fabricated image attribution on placeholder screenshots — ss-001/ss-002 claim NASA provenance for generated placeholders), 3 Medium (no Privacy/Contact page; static answer exposure undisclosed; no CSP/security headers), 4 Low. No secrets in repo; no PII in localStorage; no analytics/ads; no fake ratings.
- 2026-07-09: Release pipeline (Phase 4j) complete — GitHub Actions CI (`ci.yml`: npm ci frozen install, typecheck, lint, test, content:check, build, e2e with caching + artifacts) and deploy (`deploy.yml`: fires only on CI success on main, Vercel prod deploy + 4-step smoke test). `vercel.json` (cache + security headers), `.env.example` template, `docs/deployment.md` (preview/prod deploy, domain switch, cache strategy, rollback, smoke test). Failed builds block deploy; unknown branches never deploy to prod.
- 2026-07-09: Final code review (Phase 4k) complete — 8 independent subagents (correctness, security, game rules, test coverage, performance, accessibility, SEO, maintainability) ran read-only review. `docs/testing/final-code-review.md` records 60 deduplicated findings (P0×2, P1×11, P2×47). P0: fabricated NASA image attribution + missing `/og.png`. P1 cluster: storage/components well-built but have latent defects (no write-time validation, Modal inert scope, ToastProvider unmounted, divergent normalizeAnswer, give-up-not-locked, keywords max 8) that surface on Phase 4d wiring.
- 2026-07-09: Acceptance wrap-up (Phase 4l) complete — verdict **PASS**. In-scope fixes: (1) AGENTS.md gained 13 enforceable Agent Guardrails (bilingual); (2) duplicate storage helpers extracted to `src/storage/internal.ts`, removing `subtractDays`/`dedupe`/`dedupeList` copies from client.ts + actions.ts; (3) `scripts/lib/validators.mjs` now imports `normalizeAnswer` from `src/lib/game/match.ts` (ADR-002 single source of truth); (4) KeywordsPuzzleSchema tightened `max(8)`→`max(6)` per PRD §5.2; (5) ss-001/ss-002 attribution relabeled to `placeholder` (false NASA claim removed); (6) SECURITY-REVIEW M-3 corrected (vercel.json already ships 4 security headers; real gap is CSP + X-Frame-Options). All 6 validation commands re-run green: typecheck / lint / 203 unit tests / content:check / 3 e2e / build. Carryover P0/P1 items deferred to Phase 4d per `docs/handoff/phase-4e-4j-acceptance.md`.
- 2026-07-16: Comprehensive review fix pass (Phase 4m) complete — owner-directed "全面复盘 + 全部修改". All P0 and P1 findings from `docs/testing/final-code-review.md` resolved, plus the actionable P2 items. Storage: `saveState` write-time V2 validation + `given_up` terminal lock + defensive score/clue clamping + wrongGuesses case normalization + `:corrupted` cleanup. Modal: `createPortal` body-level inert, `useId()` title id, aria-label fallback, visible close button, hardened focus-trap selector. TimelineControls: roving tabindex with focus-following. ResultAnnouncer: `aria-live` polite (P2-38), dead `regionRef` removed. Loader: 11 new tests via real temp dirs (`GTG_DATA_DIR` hook). SEO/security: `og.jpg` + OG dimensions, `serializeJsonLd` HTML-escaping, Cloudflare `_headers` (CSP + X-Frame-Options), `.gitignore`/`.env.example`, CI audit + build assertion + Playwright cache, ADR corrections. Playwright e2e now targets the static `out/` build via zero-dep `serve-static.mjs`. Validation: 229 unit tests pass (21 files), lint clean, build compiles + typecheck passes + 17 static pages generated. See `docs/testing/final-code-review.md` § "Fixes Applied (2026-07-16)" for the full item-by-item record.
- 2026-07-17: P2 polish batch (Phase 4n) complete — owner-directed "继续". Closed the remaining opportunistic P2 items deferred after 4m. Dead-code removal: `MODE_TO_PREFIX`, `modeInfo`, `MODE_KEYS`, `breadcrumbSchema` (4 unused exports) + the now-orphaned `canonicalUrl` import. Token centralization: `--touch-target-min` (44px floor) referenced by all 5 clickable surfaces; `TOAST_AUTO_DISMISS_MS` named const. Test-helper extraction: `todayUtc`/`daysAgo` → `src/storage/__testutils__/helpers.ts` (4-way dedup, guardrail #4); `makeV1Fixture` kept local (intentionally different fixtures, guardrail #6). Honesty fixes: font tokens drop unshipped `'Inter'` (no visual change); sitemap `lastModified` → fixed `SITE_LAST_MODIFIED` constant (no more build-time crawl-signal dilution). Cleanup: 4 vestigial `zIndex as string` casts removed (React 19 accepts string). Consistency: `BottomNav` "More" → "Archive" to match `SiteHeader`. Docs: `AGENTS.md` commit conventions + document-locations inventory completed. Validation: 229 unit tests pass (21 files), lint clean, build compiles + typecheck passes + 17 static pages generated. See `docs/testing/final-code-review.md` § "P2 — Resolved (polish batch, 2026-07-17)".
- 2026-07-18: Phase 4d (game UI wiring) started — owner-approved "可以". Per guardrail #1, broken into 8 reviewable slices; slices 4d-1 and 4d-2 (pure-logic foundation, no UI) delivered as the first checkpoint. **4d-1 scoring engine** (`src/lib/game/scoring.ts`): 4 pure per-mode functions (`scoreKeywords`/`scoreEmoji`/`scoreScreenshot`/`scoreTimeline`) per PRD §7.1, all with floor 10 / give-up 0 / defensive clamp; plus `timelinePositionErrors` helper extracted for reuse by the Timeline game (guardrail #4). 38 tests pinning PRD §5.2–§5.5 worked examples + cross-mode invariants. **4d-2 puzzle selection** (`src/lib/game/select.ts`): `selectPuzzleBySeed` (for `/play/{mode}`) + `selectDailyPuzzles`/`selectDailyPuzzleForMode` (for `/daily`) using FNV-1a 32-bit hash of `(utcDate:mode)` — no `Math.random`, deterministic "same date = same puzzles for everyone" (PRD §5.1/§6.3). 18 tests including hash-determinism pins and the cross-session guarantee. **Loader hardening**: `DATA_DIR` changed from module-eval capture to per-call read so tests can swap `GTG_DATA_DIR` without `vi.resetModules()` (which was polluting subsequent tests' module cache). Validation: 285 unit tests pass (23 files, +56 new), typecheck clean, lint clean, content:check clean (8 puzzles). Next checkpoint: slices 4d-3..4d-6 (client game components + play-page wiring) pending owner review of this foundation.
- 2026-07-19: Slice 4d-3 (KeywordsGame + /play/keywords wiring) delivered. **KeywordsGame client component** (`src/components/game/KeywordsGame.tsx`): full game board per PRD §5.2 — first clue free, reveal-next (-15/keyword), wrong-guess de-duplication via `normalizeAnswer` (-10/wrong), give-up terminal, score via `scoreKeywords`, persistence via `recordModeResult` (guardrail #9 — no direct localStorage). Accessibility: `ResultAnnouncer` (aria-live polite), labeled input with auto-focus + Enter-to-submit, SR-only instructions, wrong-guess chips with `aria-label`. Storage errors → Toast (not alert); idempotent no-op → info Toast. **Page wiring** (`/play/keywords`): server component fetches daily puzzle via `selectDailyPuzzleForMode` (SSR-friendly, deterministic), passes to client `KeywordsGame`. **`utc.ts` helper** extracted for production UTC date (separate from test-only `__testutils__`). 15 tests covering: initial render, correct/alias/case-insensitive guess, wrong-guess de-dup, reveal-next counter + disable-at-last, give-up scoring (0), scoring integration (75 = 100-10-15), storage error + idempotent toast. Fixed `isCorrectGuess` signature bug (object arg, not positional). Validation: 300 unit tests pass (24 files, +15 new), typecheck clean, lint clean, content:check clean. Template established for Emoji/Screenshot/Timeline slices 4d-4..4d-6.
- 2026-07-21: Slice 4d-4 (EmojiGame + /play/emoji wiring) delivered. **EmojiGame client component** (`src/components/game/EmojiGame.tsx`): full game board per PRD §5.3 — all emojis shown upfront (they ARE the puzzle, not a revealable resource), two discrete hints (category -25, firstLetter -25) revealed sequentially, wrong-guess de-dup via `normalizeAnswer` (-20/wrong), give-up terminal, score via `scoreEmoji`, persistence via `recordModeResult`. Key difference from Keywords: no "reveal next clue" — instead a 2-stage hint button (category → firstLetter, both -25). **Page wiring** (`/play/emoji`): server component fetches daily puzzle via `selectDailyPuzzleForMode`, passes to client `EmojiGame`. 17 tests covering: emoji display, hint reveal sequence (1→2, disable at 2), correct/alias/case-insensitive guess, wrong-guess de-dup, give-up scoring (0), scoring integration (55 = 100-20-25; 50 = 100-50 for both hints), storage error + idempotent toast. Reused the KeywordsGame template (mock pattern via `vi.hoisted`, scoring integration, storage calls, a11y) — template proven portable. Validation: 317 unit tests pass (25 files, +17 new), typecheck clean, lint clean, content:check clean.
- 2026-07-25: Slice 4d-5 (ScreenshotGame + /play/screenshot wiring, P1-10 blur tiers) delivered. **GameImage extended** (`src/components/game/GameImage.tsx`): added `blurPx?: number` prop applying `filter: blur(Xpx)` to both the `<img>` and the error-fallback `<div>` — generic/reusable (guardrail #4), not screenshot-specific. **ScreenshotGame client component** (`src/components/game/ScreenshotGame.tsx`): full game board per PRD §5.4 — screenshot starts fully blurred (level 3 = 24px), each "sharpen" drops one level (3→2→1→0, -25/level, max 3), wrong-guess de-dup (-20/wrong), give-up terminal, score via `scoreScreenshot`, persistence via `recordModeResult`. Image auto-reveals fully sharp on solve/give-up so the player sees it. Blur-tier px values: `[0, 6, 14, 24]` (tuned for visible distinction). Attribution always visible (IP honesty, PRD §8). **Page wiring** (`/play/screenshot`): server component fetches daily puzzle, passes to client `ScreenshotGame`. 18 tests covering: initial max-blur render, sharpen sequence (3→2→1→0 with px assertions), sharpen disable at 0, correct/alias guess, image auto-sharp on solve/give-up, wrong-guess de-dup, give-up scoring (0), scoring integration (55 = 100-20-25; 25 = 100-75 for 3 sharpens), storage error + idempotent toast. `GameImage` mocked in tests to expose `data-blur-px` for blur-tier assertions without jsdom image loading. Validation: 335 unit tests pass (26 files, +18 new), typecheck clean, lint clean, content:check clean. P1-10 blur-tier requirement satisfied (CSS `filter: blur()` applied per level); image-compression tiers deferred to slice 4d-7.
- 2026-07-26: Slice 4d-6 (TimelineGame + /play/timeline wiring) delivered. **TimelineGame client component** (`src/components/game/TimelineGame.tsx`): full game board per PRD §5.5 — 4-6 items presented in a deterministic non-chronological shuffle (seeded mulberry32 PRNG, re-rolled until it differs from the correct order), reordering via the existing `TimelineControls` (move up/down + Arrow-key roving tabindex), "Submit order" locks + scores by total position error (-15 × Σ|Δpos|) via `scoreTimeline` + `timelinePositionErrors`, hints reveal one item's date (-10 each, max 2), give-up reveals correct chronological order with formatted CE/BCE dates (score 0, terminal). Key structural difference from the other 3 modes: no text input / wrong-guess concept — the answer is an ORDER checked once at submit. Persistence via `recordModeResult` (guardrail #9); `revealedClues` maps to `hintsUsed`, `wrongGuesses` is always `[]`. **Page wiring** (`/play/timeline`): server component fetches daily puzzle via `selectDailyPuzzleForMode`, passes to client `TimelineGame`. 16 tests covering: initial render, reorder via buttons + Arrow keys, hint reveal sequence + disable at 2, submit perfect (100) + imperfect (70 for 2-position error), give-up scoring (0) + terminal lock, scoring integration, storage error + idempotent toast. `TimelineControls` NOT mocked (real reorder exercised); `recordModeResult`/`useToast` mocked via `vi.hoisted()`. Fixes during slice: removed erroneous `target`/`aliases` from test fixture (TimelinePuzzleSchema `.omit`s those), `let seed` → `const seed` (`prefer-const`). Validation: 351 unit tests pass (27 files, +16 new), typecheck clean, lint clean, content:check clean. This closes the per-mode UI wiring set (Keywords/Emoji/Screenshot/Timeline all wired); template-proving cycle complete. Next: slice 4d-7 (P1-10 next-clue preload + image compression verification) and 4d-8 (/daily Daily Mixed Challenge page).
- 2026-07-26: Slice 4d-7 (P1-10 next-clue preload + image compression) delivered. Resolves the final P1 finding from `docs/testing/final-code-review.md`. **`usePreloadImage` hook** (`src/lib/game/usePreloadImage.ts`): reusable, SSR-safe hook that warms the browser HTTP cache via `new Image()`; supports single URL / array / undefined, `enabled` gate for on-demand preloading, `loaded` return value, cleanup on unmount, error-tolerant settlement. Wired in `ScreenshotGame` to preload the full-resolution image on mount alongside the LQIP thumbnail. Ready for `/daily` cross-mode preloading in 4d-8. **`blurSrc` LQIP field** (`ScreenshotPuzzleSchema`): optional `blurSrc` string — tiny WebP thumbnail shown as CSS `background-image` behind the main `<img>` while the full image streams in. `GameImage` already supported `blurSrc` since Phase 4g; `ScreenshotGame` now passes `puzzle.blurSrc` through. Blur thumbnails generated via Python/Pillow (no new npm dep, guardrail #3): 20×15px, Gaussian blur, WebP quality 5, output 0.06 KB each. `ss-001.json`/`ss-002.json` updated. **Image size cap enforcement** (`scripts/lib/validators.mjs`): `checkAssets()` enhanced to enforce DECISIONS.md caps — main image ≤ 80 KB, blurSrc ≤ 5 KB — and verify blurSrc file existence. `check-content.mjs` + `check-assets.mjs` updated to report `oversized` violations. Current images: ss-001=9.3KB, ss-002=8.9KB, blur thumbnails=0.06KB (all within caps). 13 new tests (11 for usePreloadImage + 2 for ScreenshotGame blurSrc wiring). Validation: 364 unit tests pass (28 files, +13 new), typecheck clean, lint clean, content:check clean (0 oversized), build 17 pages. P1-10 fully resolved. Next: slice 4d-8 (/daily Daily Mixed Challenge page).
- 2026-07-26: Slice 4d-8 (/daily Daily Mixed Challenge page) delivered — **Phase 4d (game UI wiring) complete**. All 5 MVP modes now wired. **`DailyChallenge` client component** (`src/components/game/DailyChallenge.tsx`): dashboard per PRD §5.1/§7.2 — date+streak banner with live multiplier, 4 mode cards (Keywords/Emoji/Screenshot/Timeline) each linking to `/play/{mode}` with status badge (Play/Resume/Solved/Given up) + per-mode score, summary section with `N of 4` counter, and when all 4 are resolved (solved OR given_up): daily total (Σ/400), streak multiplier, final score (total × multiplier), and ShareButton. SSR/hydration-safe (progress/streak start undefined, fill in via `useEffect` after mount — static shell renders identically server/client). Guardrail #9: no direct localStorage — reads via `getProgress`/`getStreak` from `src/storage/actions.ts`. Consumes pure `computeDailyTotal`/`streakMultiplier`/`finalDailyScore` from `src/lib/game/scoring.ts` (single source of truth, guardrail #4). `buildShareText()` plain-text payload (header + date + per-mode scores + total line). **`/daily` page** (`src/app/daily/page.tsx`): server component, SEO metadata via `buildPageMetadata` + `PAGE_METADATA.daily`, static-cacheable. **Wiring**: `PAGE_METADATA.daily` added to `site-config.ts`; `/daily` added to `sitemap.ts` (priority 0.9, daily); `Daily` link added to `SiteHeader` (desktop) + `BottomNav` (mobile, ★ icon); homepage gained a prominent Daily Challenge CTA card above Quick play. 15 new tests covering: initial render (4 cards, all "Play", 0/4, no share), mode card links, partial progress (solved scores + "Play" for unsolved, given_up badge + score 0), complete (300/400 × 1.20x = 360, share button, 4/4), all-given_up complete, share text content, streak multiplier display (1.00x/1.20x/1.50x per PRD §7.2 boundaries), today date display. Validation: 401 unit tests pass (29 files, +37 new vs. 4d-7's 364/28 — +15 DailyChallenge + 16 scoring + 6 actions streak-bundling), typecheck clean, lint clean, content:check clean (8 puzzles), build 18 pages (+1 /daily). Phase 4d closes; carryover P0-1 (screenshot asset replacement) remains pre-live.
- 2026-07-26: **P0-1 (screenshot asset replacement) resolved.** Both placeholder `.webp` files replaced with verified public-domain NASA images sourced from Wikimedia Commons. License verified per-file via the Wikimedia API `extmetadata` endpoint (`LicenseShortName: "Public domain"`). **ss-001 (Mount Everest):** `Mount_Everest_ISS008-E-6150.JPG` — NASA ISS Expedition 8 Crew / NASA Earth Observatory. Processed to 960×540 WebP (79.1 KB, q60). **ss-002 (Sahara):** `Desert_Patterns_(5182689540).jpg` — NASA Goddard Space Flight Center (via Flickr). Processed to 960×540 WebP (78.3 KB, q47). Blur LQIP thumbnails regenerated (0.1 KB each). JSON fixtures updated: `imageLicense` → `"public-domain"`, `imageAttribution` → real per-file NASA attribution + Wikimedia source. `SECURITY-REVIEW.md` H-1 upgraded from "CORRECTED" to "RESOLVED". Validation: content:check (8 valid, 0 missing, 0 oversized), typecheck, lint (0 errors), 401 tests pass, build 18 pages — all green. No P0/P1 items remain pre-launch.
- 2026-07-26: **M-2 + M-3 security review items resolved.** **M-3 (CSP + clickjacking):** confirmed already resolved in Phase 4m — `public/_headers` ships a strict CSP (`default-src 'self'; …; frame-ancestors 'none'; object-src 'none'`) + `X-Frame-Options: DENY`. Audited whether `script-src 'unsafe-inline'` could be tightened: the built HTML contains 11 Next.js RSC flight-data inline scripts per page (essential for hydration, content varies per build/page), so hashes/nonces are infeasible for static export — `'unsafe-inline'` is the standard practice. `style-src 'unsafe-inline'` is required by 335 React inline styles. Conclusion: CSP is as strict as practical. SECURITY-REVIEW.md M-3 text was stale (still said "no CSP"); updated to RESOLVED with the tightening analysis. **M-2 (answer-exposure honesty):** added an honor-system paragraph to the About page ("Like all browser-only puzzle games, the answers are bundled into the page itself — there is no server to hide them behind…"). Also fixed the stale "Content sources" copy (was "will use public-domain images… during development placeholders"; now "uses public-domain images… each image carries attribution"). SECURITY-REVIEW.md M-2 marked RESOLVED. Remaining open security item: M-1 (Privacy + Contact pages — new feature, deferred to post-4d-acceptance).
- 2026-07-27: **L-1 through L-4 security hardening complete — all Low findings resolved.** **L-1 (share route validation):** `src/app/share/[result-id]/page.tsx` now validates `resultId` against `RESULT_ID_REGEX = /^[a-z0-9]{8,32}$/` before rendering; invalid segments trigger an "Invalid Result" fallback that does not echo the raw input. 12 unit tests (`page.test.tsx`) cover valid IDs (8/32-char, placeholder), invalid IDs (too short/long, uppercase, `<script>`, path traversal, URL-encoded), and verify the raw segment is never echoed into the DOM. **L-2 (CI dependency scan):** `.github/workflows/ci.yml` upgraded from a single non-blocking audit to a two-tier enforcement — Tier 1 (blocking): `npm audit --omit=dev --audit-level=critical` fails the build on critical-severity prod vulnerabilities; Tier 2 (advisory): `npm audit --omit=dev --audit-level=high` with `continue-on-error: true` surfaces high-severity advisories in the CI log. 3 known HIGH advisories in `next`'s transitive deps (postcss ≤8.5.17, sharp <0.35.0, next itself) are documented in CI comments — the advisory range covers all released next versions, the vulnerabilities affect Server Actions/SSR/Image Optimization (not used in static export), and npm's suggested fix (downgrade to next@9.3.3) is nonsensical. `npm audit fix` applied a safe security patch (next 15.5.20 → 15.5.22, same minor version, guardrail #2 compliant). **L-3 (JSON-LD price type):** confirmed `price: 0` (number) in `structured-data.ts` — was resolved in Phase 4m. **L-4 (corrupted-state cleanup):** confirmed `saveState` clears `:corrupted` key on successful write (line 219) and `resetState` removes both keys (line 241) — was resolved in Phase 4m. SECURITY-REVIEW.md updated: all L-1–L-4 marked RESOLVED; summary table updated (High: 0, Low: 0). Validation: 413 unit tests pass (30 files, +12 new share page tests), typecheck clean, lint clean, content:check clean (8 puzzles), build 18 pages (Next.js 15.5.22). The only remaining open security item is M-1 (Privacy + Contact pages).
- 2026-07-30: **M-1 (Privacy + Contact pages) resolved — security review fully green.** Closed the final open security item. **`/privacy`** (`src/app/privacy/page.tsx`): discloses the no-account/no-analytics/no-ads model; lists exactly what `localStorage` holds under `gtg:state:v1` (daily progress, streak, stats, settings, completed/recent puzzle IDs, achievements); documents retention windows (daily 60d, heatmap 30d, recent 20 — cross-referenced to `src/storage/keys.ts`); explains the Safari-private-mode in-memory fallback; and points to the contact page for takedown inquiries. **`/contact`** (`src/app/contact/page.tsx`): designates GitHub Issues as the monitored channel for bug reports, feature requests, privacy questions, and copyright/takedown inquiries; external links use `target="_blank"` + `rel="noopener noreferrer"` (tab-nabbing / Referrer leakage prevention); sensitive security reports routed to private GitHub Security Advisories. **Footer:** `SiteFooter.tsx` now links Privacy and Contact. **Sitemap:** `sitemap.ts` adds `/privacy` and `/contact` (priority 0.3, `yearly`); `SITE_LAST_MODIFIED` bumped to 2026-07-30. **Metadata:** `PAGE_METADATA.privacy`/`.contact` added to `site-config.ts`. SECURITY-REVIEW.md M-1 marked RESOLVED; External Link Safety section updated (the Contact page introduces the first external links in `src/`). Validation: 413 unit tests pass (30 files), typecheck clean, lint clean, content:check clean (8 puzzles), build compiles + 20 static pages generated (+2: `/privacy` and `/contact`). All security findings (H-1, M-1, M-2, M-3, L-1 through L-4) are now resolved.
- 2026-07-30: **Puzzle bank expansion (Phase 4o) — text-based modes expanded 2→12 each.** Added 30 new puzzles across the three text-based modes: 10 Keywords (kw-003..kw-012, domains: science/history/nature/everyday/geography), 10 Emoji (em-003..em-012, domains: nature/science/history/everyday/geography), 10 Timeline (tl-003..tl-012, domains: science/history/technology/discovery). Total puzzle bank: 8 → 38. All new content passes the three-tier content:check: schema validation (38/38), asset check (2 referenced, 0 missing, 0 oversized), duplicate check (0 conflicts — per-mode IDs/slugs/answer-sets all disjoint). No code changes — pure data expansion. Selection tests use isolated temp dirs with fixed fixtures (`GTG_DATA_DIR`), so the new puzzles do not affect the 413-test suite (still 413/413 pass). Build verifies: 20 static pages still generated (content is fetched at runtime via `selectDailyPuzzleForMode`/`selectPuzzleBySeed`, no new routes). Screenshot mode remains at 2 puzzles (requires public-domain image assets — separate asset-sourcing task). Validation: typecheck clean, lint clean, content:check clean (38 puzzles), 413 unit tests pass (30 files), build 20 pages.
- 2026-08-01: **Screenshot mode expansion (Phase 4o cont.) — 2→6 puzzles.** Added 4 new screenshot puzzles with verified public-domain NASA images sourced from Wikimedia Commons: ss-003 Hurricane (Hurricane Isabel eye from ISS, NASA astronaut Ed Lu), ss-004 Aurora Borealis (ISS-42, NASA/Samantha Cristoforetti), ss-005 Amazon River (Space Shuttle STS078-751-094), ss-006 New York City (ISS-53 night lights). Each image: license verified via Wikimedia API `extmetadata` (`LicenseShortName: Public domain`), processed to 960×540 WebP via Python/Pillow (no new npm dep, guardrail #3), main image ≤80KB (16.9–74.4KB), blur LQIP ≤5KB (0.07–0.09KB). Per-image attribution recorded in `imageAttribution`. Total puzzle bank: 38 → 42 (Keywords 12, Emoji 12, Timeline 12, Screenshot 6). Validation: content:check 42/42 (6 referenced assets, 0 missing, 0 oversized), typecheck clean, lint clean, 413 tests pass, build 20 pages. Timeline event deduplication also applied: 6 reused historical events replaced with distinct verified alternatives (Voyager 1 1977, transatlantic flight 1919, Cai Lun paper 105, Tomlinson email 1971, Röntgen X-rays 1895, Drake oil well 1859). See `docs/handoff/phase-4o-puzzle-bank-expansion.md` for details.
- 2026-08-01: **Screenshot mode expansion (Phase 4o cont.) — 6→12 puzzles.** Added 6 new screenshot puzzles with verified public-domain NASA images, expanding Screenshot to match the other three modes at 12 each. New puzzles: ss-007 Grand Canyon (geography, Grand Canyon from space), ss-008 Great Pyramid of Giza (history, ISS-32 Pyramids at Giza), ss-009 Palm Jumeirah (everyday, ISS-47 NASA/Tim Kopra), ss-010 Solar Eclipse (science, 2024 Total Solar Eclipse NASA GRC/Jordan Salkin), ss-011 Wildfire (nature, 2018 Wildfire Smoke Across Western US via NASA WorldView), ss-012 Colosseum (history, ISS048-E-12677 NASA JSC ESRS). Each image: license verified via Wikimedia API `extmetadata` (`LicenseShortName: Public domain`), processed to 960×540 WebP via Python/Pillow (no new npm dep, guardrail #3), main image ≤80KB (4.4–78.7KB), blur LQIP ≤5KB (0.06–0.08KB). Per-image attribution recorded in `imageAttribution`. Domain distribution now spans all 5 categories (geography 5, nature 3, history 2, science 1, everyday 1). Total puzzle bank: 42 → 48 (Keywords 12, Emoji 12, Timeline 12, Screenshot 12) — the 50+ per-mode target is met for Screenshot at 12, and the overall bank is at 48. Validation: content:check 48/48 (12 referenced assets, 0 missing, 0 oversized), typecheck clean, lint clean, 413 tests pass, build 20 pages.

## Deliverables

| Document | Path | Description |
|----------|------|-------------|
| PRD | `docs/PRD.md` | Product requirements, game rules, scoring, acceptance criteria |
| Architecture | `docs/architecture.md` | Static app architecture, 14 topics with alternatives |
| ADRs | `docs/DECISIONS.md` | 8 architecture decision records |
| UX Design | `docs/ux-design.md` | Sitemap, flows, design tokens, component inventory |
| Handoff 1→2/3 | `docs/handoff/phase-1-to-2-3.md` | PM to Architect/UX handoff |
| Handoff 2→4 | `docs/handoff/phase-2-to-4.md` | Architect to Engineer handoff |
| Handoff 3→4 | `docs/handoff/phase-3-to-4.md` | UX to Engineer handoff |

## Blockers

None. Phase 4m (comprehensive review fixes) resolved all P0 and P1 architecture/code findings. Phase 4d (game UI wiring) is complete — all 5 MVP modes wired (slices 4d-1 through 4d-8). **P0-1 (screenshot asset replacement) is resolved** (2026-07-26): both placeholder images replaced with verified public-domain NASA images. **All Low security findings (L-1 through L-4) are resolved** (2026-07-27): share route validation, CI dependency scan, JSON-LD price type, and corrupted-state cleanup all closed. **M-1 (Privacy + Contact pages) is resolved** (2026-07-30): `/privacy` and `/contact` routes shipped, linked from the footer, and added to the sitemap. No P0, P1, Low, or Medium items remain — the security review is fully green.

## Carryover (content only)

Architecture/code P0/P1 findings from `docs/testing/final-code-review.md` were resolved in Phase 4m (2026-07-16). Phase 4n (2026-07-17) closed the remaining actionable P2 polish items. Phase 4d (2026-07-18 → 2026-07-26) delivered all UI wiring (slices 4d-1 through 4d-8). **P0-1 (screenshot asset replacement) was resolved on 2026-07-26** — both placeholder `.webp` files replaced with verified public-domain NASA images from Wikimedia Commons. The remaining items are deferred low-value polish:

- **P0-1 (asset replacement):** **RESOLVED** (2026-07-26) — ss-001/ss-002 now use verified public-domain NASA images (Mount Everest ISS008-E-6150, Desert Patterns 5182689540) with real per-file attribution. Blur thumbnails regenerated.
- **P1-9:** wire game components into play pages — **RESOLVED** (slices 4d-3 through 4d-6).
- **P1-10:** next-clue preload + real image compression tiers — **RESOLVED** (slice 4d-7: `usePreloadImage` hook + `blurSrc` LQIP + size-cap enforcement).
- **P2-34:** `SiteHeader`/`BottomNav` server-side active-state — deferred (architecture change, not polish).
- **Low-value P2** deferred as not worth the churn: P2-12, P2-35, P2-44/P2-45, P2-49, P2-54/P2-55. See `docs/testing/final-code-review.md` § "Deferred" for the rationale per item.

## Next Actions

1. **Do not auto-advance past Phase 4d acceptance** (AGENTS.md guardrail #13) — await owner approval of Phase 4d close-out and P0-1 resolution.
2. Owner reviews `docs/handoff/slice-4d-8-daily-challenge.md`, `docs/handoff/p0-1-asset-replacement.md`, and `docs/handoff/l1-l4-security-hardening.md` and decides: accept Phase 4d as complete → final acceptance / launch prep, or request additional polish.
3. **Puzzle bank expansion (in progress):** all four modes now at 12 puzzles each (48 total) as of 2026-08-01. Text-based modes (Keywords/Emoji/Timeline) expanded to 12 each on 2026-07-30; Screenshot mode expanded from 2→6 on 2026-08-01 and 6→12 on 2026-08-01. Remaining: continue toward the 50+ per-mode target (each mode needs 38 more), or accept 12-per-mode as sufficient for launch.
4. Fix the Deploy workflow (Cloudflare `apiToken` secret not configured — unrelated to current work).
5. Final acceptance lead runs consolidated review against AGENTS.md guardrails before launch sign-off.
